spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always
      encoding: UTF-8
jwt:
  issuer: jwsever@naver.com
  secret_key: study-springboot